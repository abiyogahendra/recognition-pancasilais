$(document).ready(function(){

  var current_fs, next_fs, previous_fs; //fieldsets
  var opacity;
  var current = 1;
  var steps = 4;
  var i;
  // setProgressBar(current);
  for( i=0; i<=step; i++ ){
    console.log(i);
      $("."+i).addClass("active");
  }
})